Python:
不可变：数字，字符串，元祖
可变：列表，字典，集合
golang：数组，slice，map
Java：
去重：set 相同& 不同^
元祖变字典:zip
深拷贝：递归复制子对象
浅拷贝：非递归复制子对象
测试用例：功能，UI，易用性，性能，安全，接口，兼容性，可移植
数据结构：链表，栈和队列，树，图
web过程：host，缓存，dns，tcp，报文，响应，html，渲染
三：syn; syn+ack; ack报文; 收到
四：fin; ack; fin; ack
拥塞控制：慢启动；快重传；拥塞避免；快恢复
进程调度：先来；短作业；高优先；时间片；多级反馈
进程：可拥有资源，可独立调度和分配（程序段，相关数据段，PCB）动并独异
线程：CPU调度和分派
同步机制：互斥锁，信号量，事件
高级通信工具：共享存储器；管道；消息传递
死锁：竞争；顺序 条件：互斥；请求和保持；不剥夺；环路等待
页面置换：OPT，FIFO，LRU,
栈：先进后出 队列：先进先出






